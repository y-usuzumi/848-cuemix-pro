# TODO

- [ ] AVDECC push updates: the 848 accepts an HTTP `CONNECT` tunnel on TCP 17221 and advertises DNS-SD `Version=1`. It replies to a standards-defined v0 `ENTITY_ID_REQUEST` with a nonzero reserved field (`0xffff`). The app can use that payload only as a candidate in one validated, read-only AEM `READ_DESCRIPTOR` exchange. Entity and Configuration 0 reads now work, proving one Audio Unit, 17 Stream Inputs, 17 Stream Outputs, one standard Control, and one Clock Domain. The next step is to map a source-backed registration mechanism for unsolicited notifications, renew it when required, and retain HTTP polling as a recovery fallback.
- [ ] Hardware A/B/C monitor selection and groups: `ABC Monitor L/R` map to Audio Clusters `23/24`, but the only standard Control (`0x001a:0`) is an unrelated `IDENTIFY` value with no signal association. Map the vendor-specific A/B/C selection and output-assignment command before exposing a write control. The HTTP compatibility datastore exposes analog output trims but not a safe A/B/C control mapping.
- [ ] Native IPv6 mDNS: enumerate multicast-capable interfaces and send scoped `_avdecc._tcp.local` queries to `ff02::fb`, removing the current Avahi dependency for IPv6-only discovery.
