# TODO

- [x] Persistent transport for every slider: mixer/aux/Reverb levels, pans and masters now join monitor and output writes on the meter worker; Mic/Line gains use typed one-byte `1389`/`13b2` setters there too. Preserve advertised-index/range checks, batch conflicts, no-op filtering, one deadline and fresh readback. Cache target identity per worker, remove the standalone console writer, and retain recovery polling. Synthetic TCP tests alternate all slider types over one connection with uninterrupted meter/ACK sequences and cover failure without retry; route/binding tests check the persistent API. Read-only hardware gains match HTTP, while actual gain setters remain for controlled user validation.

- [x] Output-trim timeout repair: line/headphone slider writes now share the persistent meter/monitor worker instead of closing it and opening a fresh session for every drag position. Rediscover only advertised indices, retain one payload per stereo headphone pair, skip no-ops, verify fresh readback and remove automatic retries of uncertain writes. One deadline bounds queueing through readback. Synthetic TCP and server-route tests cover repeated updates, meter continuity, sparse stereo pairs, rejection, expiry, lost acknowledgements and mismatched readback. Live write timing remains for user-controlled validation.

- [x] Preserve Mute/Mono/Talk button children during live meter updates so pointer clicks are not lost. Update only changed state text, route label hits to the button, and cover repeated refreshes plus simulated Mono on/off clicks.

- [x] Present headphone outputs as two compact vertical strips with larger L/R meters and output-style faders. Keep each stereo pair linked, preserve save/readback behavior, and fit the sidebar on laptop screens.

- [x] Let input, output, mixer and aux banks use the full available width without an eight-channel cap, while retaining compact strip dimensions and paging when needed.

- [x] Cap hardware and mixer strip dimensions on large screens, including filtered results and partial banks; keep controls together at the top instead of stretching them across the workspace.

- [x] Implement the approved compact console design: five workspaces, input/output banks, paged mixer and aux strips, a persistent monitor/headphone panel, and shared Patchbay/Matrix navigation with the existing routing review tray. Responsive sizing preserves hardware controls and pending edits while banking; local System/Light/Dark themes need no external assets. The shared Outputs recovery poll and meter stream now keep monitoring current in every workspace. Offline coverage includes banking, edit preservation, monitor state outside Outputs, and a complete synthetic browser fixture. Hardware validation remains read-only.

- [x] Front-panel Mono button beside Mute and Talk: installed-client `kMonoEnable` maps to boolean `139a:0`, independently advertised as off by the attached 848. The button uses fresh conflict checks/readback and follows hardware state through SSE and recovery polling. It changes the device's mono latch, preserving trims, routes and other controls. Synthetic checks cover both directions, malformed inventory, stale batches, rejection and external updates; audible hardware verification remains manual.
- [x] Front-panel Mute and Talk buttons in Outputs → Monitoring: source-backed `kMuteEnable` (`139b:0`) and `kTalkbackEnable` (`13a3:0`) setters use the persistent monitor connection, fresh conflict checks/readback and live state stream. Explicit click-on/click-off controls preserve level, speaker selection and talkback setup; setup remains in CueMix Pro's Home tab. Tests cover wire bytes, invalid inventory, conflicts, coalescing, rejection and external updates. Live verification is read-only; both hardware setters and audible behavior still need user-controlled validation.
- [x] Consistent meter appearance and motion: Mixing and Aux Mixing reuse the same fill styling as Inputs, Outputs and diagnostics. A fixed full-height green-to-amber-to-red gradient is clipped at the current level, with an 80-millisecond linear transition of the clip edge; the colors stay anchored to the meter scale as levels change.
- [x] Monitor display reconciliation after dragging: remove the pointer-held display freeze; only actual queued/in-flight changes override confirmed state. A lost pointer-release event can no longer leave a stale level visible after readback. The regression reproduces a -31 dB preview over a confirmed -30 dB result and checks both the slider and label recover without pointerup or blur.
- [x] Independent monitor cache recovery: live comparison found UI/cache -31 dB while a separate hardware inventory and CueMix Pro both showed -45 dB. Empty incremental responses do not verify current device state. Explicit refreshes, pre-write checks and readback now reload the full inventory on the existing session; a 500 ms background interval plus read time recovers missed events. Meter reads continue between pages. Both open pages recovered to -45 without reloading; 392 SSE events over 23.5 seconds had no errors and a 66.6 ms maximum gap. Tests cover missing notifications, stale-write rejection, real readback and event/page ordering. Missing-event/controller scope still needs investigation.

- [ ] AVDECC push updates: the 848 accepts an HTTP `CONNECT` tunnel on TCP 17221 and advertises DNS-SD `Version=1`. A passive 1.5-second tunnel emitted no initial frames; that does not establish registered-notification behavior. It replies to a standards-defined v0 `ENTITY_ID_REQUEST` with a nonzero reserved field (`0xffff`). The app can use that payload only as a candidate in one validated, read-only AEM `READ_DESCRIPTOR` exchange. Entity and Configuration 0 reads now work, proving one Audio Unit, 17 Stream Inputs, 17 Stream Outputs, one standard Control, and one Clock Domain. Standard AEM registration is command `0x0024` with no command-specific fields, but do not send it until the 848's proxy identity lifecycle and notification behavior are mapped. Then renew it when required and retain HTTP polling as a recovery fallback.
- [ ] Vendor state-delivery mapping: the standard passive `avdecc-probe --listen-ms` captures during a Mic-label change and idle period contained only ADP `ENTITY_AVAILABLE`; that remains insufficient for standard AVDECC notification registration. However, passive Windows captures map CueMix's protocol-`00:01:f2:00:00:01` lifecycle: every ~5 seconds it sends a re-arm whose two-byte payload is the preceding re-arm sequence, receiving an empty response. A front-panel property event uses the outstanding request sequence, followed by CueMix acknowledgement/re-arm traffic. The device-open trace maps creation: after HTTP CONNECT, proxy identity, and read-only descriptor discovery, CueMix issues an empty vendor request and receives 198 self-delimiting state pages (9,307 u16 id/u16 index/u8 size/value records) before the terminal empty response starts the idle cadence. A capture left on CueMix's DISCOVERY screen had only the old socket's FIN teardown and no new TCP 17221 session, so device discovery is not session creation. The worker loads the bounded initial state once, maintains it with incremental re-arms plus independent full inventory reconciliation, and applies events matching the outstanding state sequence. Empty re-arm replies can leave cached values stale; missed-event/controller scope still needs mapping. All slider and console batch writes now reuse that session with independent inventory checks and readback; only legacy presets and line polarity retain the separate initial-state lifecycle. The expanded console mappings and evidence are documented separately. Each control requires acknowledgement and never sends setters while reading or polling. Continue mapping controller scope and notification semantics, retaining polling and withholding standard registration.
- [x] Patchbay, routing, mixing and aux mixing: searchable routing list and paged matrix share a staged review tray (up to 32 edits). Mixer views discover input links and Main/aux/Reverb buses, expose level/pan, shared input mute/solo, masters and bus-wide pre/post sends, and retain live meters and recovery polling. Read-only router correlation and installed CueMix Pro converters establish Q24 gain/pan and packed bank/channel indices; input solo is `03fa`, mute is `03fb`. Fader indices address mixer slots: the previous source-family interpretation of Line In 5–6 index `0x10` is superseded by its current slots 16–17. Every batch validates a fresh snapshot before writing, requires matched successful acknowledgements, and verifies readback. See [protocol evidence](docs/console-protocol.md).
- [ ] Expanded console hardware validation: new routing/pan/mute/solo/master/pre-post writes were tested against synthetic fixtures, not live audio hardware. Validate firmware behavior, stereo propagation, external-controller races and interrupted batches in a controlled session. Stereo-link editing, DSP effects, native bus aliases and AVB stream connection management remain future work; retain polling until vendor notifications are understood.
- [x] Meter channel encoding: each big-endian u16 meter word is two independent u8 attenuation magnitudes, not one Q8.8 value. The high byte is the first/left channel, the low byte is the second/right channel, values `0x00` through `0xfe` use 0.5 dB attenuation steps, and `0xff` is silence. Repeated values such as `0xb4b4` initially hid the channel structure; live unequal values such as `0x6a6f`, exact per-channel path selectors, and the line-output path pairs make it explicit. Comparing a microphone tap against CueMix Pro establishes the scale: a raw value around 48 aligns with −24 dBFS, not −48 dBFS. The browser decodes both channels and uses a −127 to 0 dBFS display range.
- [x] Physical input meter mapping: `0x138c:0` contains two words/four channels for Mic / Inst 1-4, and `0x13ac:0` contains four words/eight channels for Line Inputs 5-12. The formerly described “pair aggregate” is the packed high/low channel representation. The matching Mix In words at `0x13ad:0`, `0x13ad:0x20`, and `0x13ad:0x40` are still separate signal stages.
- [x] Device-defined channel labels: Mic input bank `0`, Analog input bank `1`, and Analog output bank `0` expose per-channel `ch/<index>/name` values. Control-strip headings and mapped meter names use those nonblank values, falling back to the physical `Mic / Inst N`, `Line In N`, and `Line Out N` names only when the device label is blank. Clicking any Mic, Line In, or Line Out heading opens the shared name editor inline; Enter or blur writes the corresponding bank's `ch/<index>/name`, and Escape cancels without a write.
- [x] Line-input gain and polarity controls: the tested 848 advertises eight channels in HTTP `ext/ibank/1` (`Analog`) with gain range `0:20`; its current gains `[20,20,0,0,0,0,0,0]` exactly match one-byte vendor property `0x13b2` at indices `0`-`7`. HTTP exposes no line-input phase field, while adjacent property `0x13b3` has eight matching boolean records and the installed CueMix binary has distinct `kLineInGain` and `kLineInPhase` models. The Inputs tab requires both sorted index sets to match and labels them Line Inputs 5-12. Gain follows the Mic-preamp path: immediate local feedback, coalesced persistent-session writes during dragging through `/api/inputs/gain`, and the 750-millisecond HTTP recovery refresh. Both gain banks verify independent vendor readback. Polarity retains its separate session and an explicit protocol-`...:03` one-byte `0x13b3` write. The exact polarity write and concurrent-controller behavior remain to be confirmed; automated verification is read-only.
- [x] Continuous sliders: mic/line gains, line/headphone trims, mixer/aux levels, bus masters and pan send during dragging with an 80 ms coalescing window, one request in flight and a final-position flush. Monitor keeps its existing 60 ms synchronization. Mixer readback preserves the active fader and advances buffered expected values only through verified writes. Failures clear buffered values and block the rest of that gesture; device switches cancel unsent changes. Synthetic queue, actual binding and console race tests cover input-only writes, slow responses, coalescing, final-value deduplication, stereo master addressing, conflict handling and device changes without writing audio hardware.
- [x] Generic line-output trims: the compatibility HTTP output bank advertises 12 channels and accepts writes but does not track CueMix Pro trim changes. In a known state, the bounded vendor snapshot exposed `0x1388:0 = 35` for Line Out 1 at -35 dB and `0x1388:3 = 42` for Line Out 4 at -42 dB; Line Outs 1-2 were monitor-group members during the comparison. Discover the full sorted `0x1388` record set instead of assuming a model-specific count, map attenuation `100` to negative infinity, and write only a freshly discovered index after an explicit user action. Line and headphone reads share one slower snapshot poll. The exact `0x1388` write and concurrent external-controller behavior remain to be confirmed.
- [x] Generic headphone trims: the 848 HTTP datastore has no headphone-volume key. Its bounded initial vendor-state snapshot exposes four one-byte `0x13b7` values at consecutive indices `0`-`3`; the observed `[100,100,50,50]` exactly matches the front-panel state of Phones 1 at negative infinity and Phones 2 at -50 dB, with `100` as the negative-infinity sentinel. `0x1388` has 12 values matching Line Outs 1-12, and the superficially plausible four-value `0x139d` remains `[0,0,0,0]` at those headphone settings. Static AEM strings independently identify `Headphones 1 L/R` and `Headphones 2 L/R`, and the installed CueMix Pro binary has separate `kHeadphoneTrim` and `kLineOutTrim` models using the same output-trim converter. The UI discovers an even number of consecutive `0x13b7` channels, groups them as linked stereo pairs, and therefore avoids a model table for the two-phone 848/10pre and one-phone 16A. One explicit user request writes both discovered pair members as protocol-`...:03` records `13:b7:<u16 index>:01:<attenuation>`; values are limited to negative infinity or integer `-99` through `0` dB and are never written during verification. This exact `0x13b7` write remains to be confirmed by a controlled user action or capture; concurrent external-controller behavior also remains unmapped. Retain the slower read-only snapshot poll until the notification lifecycle is implemented.
- [x] Routing-aware output meters: initial-state property `0x93ac` has one four-byte meter path for every discovered line output; the value is `u16 property/u8 record/u8 channel`. On the tested 848, Line Outs 1-2 select channels 0-1 of `0x13ad:1`, while Line Outs 3-12 select channels 2-11 of `0x13ad:0`, exactly matching their current HTTP source routes. Property `0x13b4` supplies one path per Phones output; the selected channel and the following channel form its L/R meter. The UI discovers these paths rather than hardcoding current routes.
- [x] Real-time browser meters: the persistent meter worker publishes every completed two-page snapshot to a condition-backed feed at a target 5-millisecond interval. Inputs, Outputs, and Mixer consume that feed through `/api/mixer/meters/events` Server-Sent Events and coalesce DOM work with `requestAnimationFrame`; they no longer issue 125-millisecond HTTP snapshot polls or repeat `/datastore` identity reads. The one-shot `/api/mixer/meters` endpoint now returns an existing worker snapshot without contacting the device. Meter strips use bottom-up vertical bars and share a piecewise −127-to-0 dBFS scale with evenly spaced marks at −∞, −48, −36, −24, −12, −6, −3, and clip; Phones use adjacent L/R bars. Each primary lane also has a thin peak marker that updates immediately for a louder sample and holds for one second after the live level falls. Inventory reads share that session and its incremental state cache; all slider and console batch writes reuse it, while legacy presets and line polarity still close it first. Background inventory reconciliation preserves meter delivery even when incremental state events are missing. Retain polling as the recovery path until AVDECC unsolicited state notifications are implemented.
- [x] Refresh continuity: console, line-input and output inventory reads use the existing meter session with a total deadline and a 256-page bound. An empty initial state request loads the inventory; explicit reads independently reload the inventory, and a 500 ms background interval plus scan time recovers missed incremental events. ACKs refer to the last state response sequence, including terminal empty responses, even when meters or writes intervene. Background reads keep controls enabled and preserve focused edits, queued edits retain their original conflict values, and brief failures use the last confirmed snapshot before pausing editing after 15 seconds. Write readback failures pause immediately. A timed-out stop retains its worker registration until closure, preventing overlapping sessions. Local TCP and UI race tests cover these paths; read-only release verification completed 20 refreshes and 702 SSE updates over 42.1 seconds with no errors/disconnections and a 62.2 ms maximum meter gap. See [protocol evidence](docs/console-protocol.md).
- [x] Passive vendor-state decoder: `avdecc-probe` recognizes bounded received `...:01` property records for A/B/C selection, Monitor Group membership, MUTE, and the still-unknown front-panel event. It only decodes received frames and never sends the vendor lifecycle messages.
- [x] Passive AVDECC observation: `avdecc-probe --listen-ms` timestamps complete v0 frames from the start of the bounded listen window and decodes ADP entity announcements. It makes repeated no-registration comparisons reproducible while HTTP polling remains the recovery mechanism.
- [x] CueMix Pro passive-change test: changing and restoring a Mic label during a 15-second `--listen-ms` window emitted only ADP `ENTITY_AVAILABLE` heartbeats at the five-second cadence. No passive control notification was observed.
- [x] Hardware A/B/C monitor selection and groups: Outputs now provides the captured Off/A/B/C/All selection masks (`13b6:0`), the single Monitor Group membership mask (`1394:0`, mapped Line Outs 1–12), and shared monitor level (`1393:0`, installed OutputTrimConverter). The previously unmapped `02` is the two-byte value length. AEM Audio Clusters 23/24 corroborate the shared stereo input; installed `kProMonitorBankPatch` and source-stream encoders map input routes `93b9:0/1` and A/B/C source paths `13b9`, banks 0/1/2, channels 0/1. Expandable setup stages source and speaker-pair assignments for review. Existing Patchbay and Routing also expose these channels. The initial view shares the Outputs snapshot; subsequent monitor state travels with meter SSE events. Monitor-only writes reuse the session with independent fresh-inventory expected-byte checks and readback without retries. A+B, A+C and B+C shortcuts now send the corresponding single masks 3/5/6; the user confirms two-button selection on the device, and the existing bit mapping supplies these combinations. Mute `139b` is displayed read-only. See [mapping and validation evidence](docs/console-protocol.md#abc-monitoring).
- [ ] Monitor hardware listening validation: confirm the newly implemented master-level and ABC routing setters, relative trims, switching between configured pairs, and concurrent front-panel changes during a controlled user listening session. All implementation verification was read-only on hardware; simulated writes cover the UI and unit tests. Validate Mute and Talk enable/disable and their audible targets; their setter mappings now have installed-client evidence. Validate the derived two-group masks 3/5/6 in the same controlled listening session; their automated checks use a simulator.
- [ ] PipeWire 848 volume quirk: the 848's USB `Audio Out` mixer can remain full scale while the 128-channel PipeWire sink restores to zero. The 848 exposes only 16 UAC playback-volume controls, and its 128-channel native PipeWire nodes are presented as only 32 channels through Pulse. Live diagnostics show that 128-channel F32P DSP PortConfig replaces the playback adapter node, exposes 128 physical input ports, and removes node-level `softVolumes`; the Pulse view therefore remains locked at a cosmetic 0%. The KDE `848 Multichannel` slider can silence the adapter while its DSP links and ALSA mixer remain healthy. The capture compatibility source can likewise sit at negative infinity while its upstream microphone meter moves. `tools/enable-motu-volume-lock.sh` now applies MOTU-only `channelmix.lock-volumes` protection to both playback and capture nodes at creation, and disables stale property restoration for both. `tools/recover-motu-audio.sh --native-dsp` requires the playback protection while verifying the full-scale direct FL/FR path. One live KDE slider change no longer silences playback. Confirm repeated playback and capture slider changes and restart/suspend recovery before making native DSP the default; `--native-volume` remains a compatibility alias.
- [x] Native IPv4 mDNS: enumerate active, multicast-capable IPv4 source addresses on Linux and Windows, bind one query socket to each, and send `_avdecc._tcp.local` to `224.0.0.251`. This prevents Windows from selecting only a default adapter when the 848 is on the MOTU or Realtek network. A hardware run on Windows now discovers the tested 848 at `192.168.4.166` with its expected AVDECC TXT records.
- [x] Device home page on no-argument launch: bare `cuemix-848` and hostless `serve` show discovered devices, rescan without restarting, and accept manual IPv4/IPv6 entry. Token-protected connect reads the datastore identity before admitting an IP for the server session, retains the input on failure and opens the console on success. Discovery failures leave manual connection available; rescans preserve addresses already in use. The console has a Devices return link. Fixed-host mode retains its single-host boundary. Synthetic tests cover authorization, address parsing, discovery recovery, failed responses, read-only connection, and browser submission/navigation.
- [x] Recent manual connections on the home page: save up to eight successful canonical addresses in browser storage, newest first without duplicates. Click to revalidate and reconnect, or clear the list. Failed attempts and discovered-device selections do not populate history. Tests cover persistence, ordering, limits, reconnect failures, clearing, invalid stored data, and unavailable storage.
- [x] Editable dB readouts for mic/line gain, line/headphone output trim, monitor level, mixer channels, aux sends and bus masters. Enter or blur commits through the existing persistent slider queues; Escape cancels. Range/step validation rejects invalid input, supported controls accept `-inf`, and polling preserves unfinished edits. Synthetic tests cover parsing, cancellation, no-op edits, disabled controls, linked headphone values and all write paths.
- [x] Linux IPv6 mDNS: enumerate up, multicast-capable non-loopback Linux interfaces and send scoped `_avdecc._tcp.local` queries to `ff02::fb` under the same discovery deadline as IPv4. Preserve the receiving interface in link-local answer addresses and accept bracketed scoped IPv6 hosts in HTTP and AVDECC Proxy clients, removing the Avahi dependency.
- [ ] Windows IPv6 mDNS: enumerate eligible Windows IPv6 interfaces and issue scoped `ff02::fb` queries, preserving the interface scope in link-local answer addresses.

- [x] Responsive monitor synchronization: level changes save during dragging with a 60 ms coalescing window and only the latest unsent value per control. Level, selection, membership and mute updates travel with the meter stream and update controls in place, preserving focus and pending edits. Delayed responses cannot replace newer revisions; device switches cancel queued work. Live read-only verification delivered 345 monitor/meter events in 20.7 seconds alongside 20 inventory GETs, with no errors and a 61.4 ms maximum event gap. Synthetic TCP and browser checks cover write/readback, coalescing, conflicts and failures without changing audio hardware. Physical-knob and live-write end-to-end latency remain to be measured in use.
